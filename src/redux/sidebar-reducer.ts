import {AllActionsType, StateType} from "./store";

let initialState:{} = {} // wtf ???

export const sidebarReducer = (state: {} = initialState, action: AllActionsType):StateType => {
    return <StateType>state
}
